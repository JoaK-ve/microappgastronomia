// Versión de la APLICACIÓN (producto), no de recetas — conceptos
// independientes. Fuente única de verdad: package.json#version,
// inyectada por vite.config.ts como __APP_VERSION__.
export const APP_VERSION = __APP_VERSION__
export const APP_VERSION_DISPLAY = `V${APP_VERSION}`
