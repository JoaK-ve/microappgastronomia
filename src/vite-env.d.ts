/// <reference types="vite/client" />

// Inyectada por vite.config.ts (define) desde package.json#version —
// única fuente de verdad para la versión de la aplicación.
declare const __APP_VERSION__: string
